node {
    stage('Clone repo'){
        checkout scm
    }
    
    stage('Build'){
        sh 'env > env.txt' 
        for (String i : readFile('env.txt').split("\r?\n")) {
            println i
        }
    }
}
